
dependencies:
  pre:
    - go get github.com/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME
  post:
    - make install
#    - docker run --publish 6060:8080 --name test --rm $CIRCLE_PROJECT_REPONAME
