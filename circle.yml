machine:
  pre:
    - curl -o go.tar.gz -sL https://storage.googleapis.com/golang/go1.6.2.src.tar.gz
    - sudo rm -rf /usr/local/go
    - sudo tar -C /usr/local -xzf go.tar.gz
    - sudo chmod a+w /usr/local/go/src/

dependencies:
  post:
    - make build
